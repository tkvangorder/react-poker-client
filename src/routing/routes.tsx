import { createBrowserRouter } from "react-router-dom";
import TitlePage from "../components/TitlePage";
import { LoginForm } from "../user";

//createBrowserRouter([{ path: "/", element: <TitlePage /> }]);
